import React from 'react'
import { storiesOf } from '@storybook/react'
import Button from './component/button'

storiesOf('表单 | Form', module)
.add('组价测试', () => <Component />)
.add('Button', () => <Button />)